import { styled } from 'styled-components';

export const EditNickNameWrapper = styled.form`
  display: flex;
  flex-direction: column;
  align-items: center;
`;

export const ButtonWrapper = styled.div`
  position: fixed;
  bottom: 1rem;
`;
